var fs = require('fs');
var test = require('../test.js');
var cases = [];

module.exports = test(cases, __dirname);


var tests = fs.readdirSync(__dirname);
while (tests.length > 0) {
  var step = require('./' + tests.shift());
  if (step !== module.exports) {
    cases.push(step);
  }
}
