{% set methods = data.services[0].methods %}


/**
 * @enum {string}
 */
{{ data.namespace }}.Api = {
  {% for method in methods -%}
  '{{ method.name | camel_to_snake(upper=true) }}': '{{ method.name }}'
  {%- if not loop.last %}, {% endif %}
  {% endfor -%}
};