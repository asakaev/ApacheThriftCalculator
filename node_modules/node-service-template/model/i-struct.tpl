{% import 'types.tpl' as types with context %}


/**
 * @interface
 */
{{ data.namespace }}.model.I{{ structure.name }} =
  function() {};


/**
 * @return {!{{ data.namespace }}.model.{{ structure.name }}}
 */
{{ data.namespace }}.model.I{{ structure.name }}.prototype.toThrift =
  function() {};
