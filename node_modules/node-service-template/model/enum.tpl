{% import 'types.tpl' as types with context %}


/**
 * @enum {number}
 */
{{ data.namespace }}.model.{{ enum.name }} = {
  {% for enumerator in enum.fields -%}
  '{{enumerator.name}}': {{enumerator.value}}
  {%- if not loop.last %}, {% endif %}
  {% endfor -%}
};

