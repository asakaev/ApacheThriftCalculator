
/**
 * @type {!{{ data.namespace }}.model.IBuilder}
 */
{{ data.namespace }}.model.__BUILDER =
  new {{ data.namespace }}.model.Builder();


/**
 * @param {!{{ data.namespace }}.model.IBuilder} builder .
 */
{{ data.namespace }}.model.setModelBuilder = function(builder){
  {{ data.namespace }}.model.__BUILDER = builder;
};


/**
 * @return {!{{ data.namespace }}.model.IBuilder}
 */
{{ data.namespace }}.model.getModelBuilder = function(){
  return {{ data.namespace }}.model.__BUILDER;
};